Online Shopping Cart
Concepts: Classes, Objects, Inheritance, Polymorphism, Encapsulation, Interfaces

Classes and Methods:
Product Class:

Fields: productId, name, price, quantity
Methods: getProductId(), getName(), getPrice(), getQuantity(), setQuantity(int quantity)
Cart Class:

Fields: cartId, products (list of Product)
Methods: addProduct(Product product), removeProduct(Product product), getTotalPrice(), getCartId(), getProducts()
Order Class:

Fields: orderId, cart, status
Methods: placeOrder(), cancelOrder(), getOrderId(), getCart(), getStatus(), setStatus(String status)
Payment Interface:

Methods: processPayment(double amount)
CreditCardPayment Class (implements Payment):

Fields: cardNumber, expiryDate
Methods: processPayment(double amount), getCardNumber(), getExpiryDate()
PayPalPayment Class (implements Payment):

Fields: email
Methods: processPayment(double amount), getEmail()