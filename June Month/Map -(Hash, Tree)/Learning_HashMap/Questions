🔍 Advanced Scenarios on Java HashMap / Map Behavior
What happens internally when two keys have the same hash code in a HashMap?

How does HashMap handle null keys and null values? Can there be multiple null values? What about multiple null keys?

What is the impact of a poorly implemented hashCode() or equals() method on HashMap behavior?

How does HashMap behave during iteration if the map is structurally modified (i.e., adding/removing entries)?

What is hash collision chaining in a HashMap, and how does Java resolve it?

What changes were introduced in Java 8 regarding the internal structure of HashMap (e.g., switching to TreeNode)?

In what scenario does HashMap degrade to O(n) performance? Why?

How does the load factor affect resizing in a HashMap?

What are the thread-safety issues with using HashMap in a multi-threaded environment? What inconsistent states can occur?

How is LinkedHashMap different from HashMap in terms of entry ordering and iteration?

What is the difference between identity-based comparison (like IdentityHashMap) and equality-based comparison in regular HashMap?

What can go wrong if mutable objects are used as keys in a HashMap?

How do weak keys work in WeakHashMap and how does garbage collection affect it?

What is the consequence of overriding only hashCode() but not equals() (or vice versa) in a key class used in HashMap?

What happens when a HashMap reaches its threshold size due to load factor, and how is rehashing done internally?

Can a ConcurrentModificationException be thrown while reading a HashMap using forEach() or entrySet()?

Why is Map.of(...) immutable and how does it behave when you try to put or remove an entry?

What internal mechanism is used to maintain insertion or access order in LinkedHashMap?

How does Java’s computeIfAbsent() or merge() method behave in concurrent access scenarios?

Can two objects with equal content but different hash codes be used as keys in a HashMap? What would be the result?

