🔥 Core Collection Traps

Can a HashMap store duplicate keys? What exactly happens internally when it tries?

Why does HashMap allow one null key but Hashtable allows none?

Why does ConcurrentHashMap not allow null key or value?

What happens if two keys have the same hashCode but different equals()?

What happens if two keys have different hashCode but equals() returns true?

Why is overriding equals() without hashCode() dangerous in HashMap?

Why is hashCode() allowed to return negative values?

Why is bucket size in HashMap always a power of 2?

How does HashMap convert a hashCode into a bucket index?

Why did Java 8 introduce treeification in HashMap?

🧠 HashMap Internal & Edge Cases

What happens when HashMap capacity exceeds threshold?

Why is default load factor 0.75?

What happens if initial capacity is set to 3?

Can HashMap ever resize downwards?

Why is rehashing expensive?

What happens to HashMap during resize if two threads modify it concurrently?

Why can HashMap cause infinite loop in Java 7 under concurrency?

What is the role of modCount in collections?

Why does ConcurrentModificationException not occur always?

Why is it called fail-fast and not fail-safe?

🔄 Iteration & Modification

Why does modifying a collection while iterating cause ConcurrentModificationException?

Why does Iterator.remove() work but collection.remove() fails during iteration?

Why does CopyOnWriteArrayList not throw ConcurrentModificationException?

Why does enhanced for-loop internally use Iterator?

Can we modify a list inside forEach() lambda?

🌲 TreeMap & Sorting Traps

Why does TreeMap throw ClassCastException?

What happens if compareTo() returns 0 for two different objects?

Can TreeMap contain null keys?

Why does TreeMap use Red-Black Tree and not AVL?

What happens if comparator logic is inconsistent with equals?

🧵 Concurrency & Performance

Why is Collections.synchronizedMap() slower than ConcurrentHashMap?

How does ConcurrentHashMap avoid full locking?

Why is read operation non-blocking in ConcurrentHashMap?

What is the role of CAS in ConcurrentHashMap?

Why is size() not accurate in ConcurrentHashMap?

Why does ConcurrentHashMap not support locking on entire map?

What happens if two threads update the same bucket in ConcurrentHashMap?

📦 List & Set Corner Cases

Why does Arrays.asList() throw UnsupportedOperationException on add/remove?

Why is ArrayList index-based access fast but insertion slow?

Why is LinkedList bad for random access?

Why does HashSet not allow duplicates internally?

Can HashSet contain duplicate objects with different references?

Why does LinkedHashSet preserve insertion order?

How does LinkedHashMap maintain access order?

🧩 Equality & Object Design

Why must keys in HashMap be immutable?

What happens if a key is mutated after insertion?

Why does String work perfectly as a HashMap key?

Why is String hashCode cached?

What happens if equals() is expensive?

Why should equals() be symmetric, transitive, and consistent?

🧪 Scenario-Based Tricky Questions

Map contains 1 million entries — how to iterate with minimal memory?

How to safely remove elements from a Map while iterating?

Why does map.get() sometimes return null even when key exists?

Can two different objects exist in HashMap but size still be 1?

Why does TreeMap size not increase for some put operations?

Can HashMap guarantee insertion order?

Can LinkedHashMap behave like LRU cache?

Why is EnumMap faster than HashMap?

Why is WeakHashMap used in caching?

What happens to entries in WeakHashMap during GC?

🔥 Ultra-Tricky (Senior Level)

Why does HashMap use (n-1) & hash instead of %?

Why is equals() not used before hashCode()?

Why does HashMap compare hash first, then equals?

Can hash collisions be completely avoided?

Why does TreeMap not use hashCode at all?

Why is Set preferred over List in some performance-critical code?

Why does remove() in ArrayList shift elements?

Why does ConcurrentHashMap iteration reflect partial updates?

Can fail-fast behavior be disabled?

Why is iteration order not guaranteed in HashMap?